version: "3.9"
services:

  # -----------------------------
  # FastAPI Management API
  # -----------------------------
  workflow_mgt_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wf-mgt-api

    env_file:
      - .env

    ports:
      - "8000:8000"

    restart: unless-stopped